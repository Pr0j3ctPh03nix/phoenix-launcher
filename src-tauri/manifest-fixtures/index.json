{
  "_note": "Conformance fixtures for manifest readers, generated by tools/gen_fixtures.py. Feed each `file` to the reader and assert `expect`. Do not edit by hand.",
  "schema": 3,
  "future_schema": 4,
  "expectations": {
    "accept": "The reader parses the manifest and proceeds normally.",
    "refuse:schema": "Rejected specifically because the schema is unsupported, with a message telling the user to update. NOT a parse error.",
    "refuse:invalid": "The schema is supported but the document violates a producer guarantee (B1-B8 in docs/manifest-format-v3.md) and cannot be acted on. A broken release — NOT an 'update the app' message, and not a schema refusal.",
    "refuse:codec": "The schema is supported but a bundle names a codec the reader cannot decode. Same user-facing outcome as refuse:schema (tell the user to update); different detection point."
  },
  "cases": [
    {
      "file": "current.json",
      "expect": "accept",
      "why": "The current schema with every feature present: a raw entry, a multi-member bundle, a one-member bundle, a zero-byte entry and a BUNDLED OPTION VARIANT. A reader that rejects this is broken outright."
    },
    {
      "file": "schema2-options.json",
      "expect": "accept",
      "why": "Exactly what the shim producer emits today — schema 2, every entry named, no bundles. The shim never cuts over to 3, so a schema-3 reader that only handles bundled documents would break the repo it updates from on every launch."
    },
    {
      "file": "schema3-no-bundles.json",
      "expect": "accept",
      "why": "Schema 3 with no `bundles` key at all. Absent MUST mean none — a schema-3 document that bundles nothing is exactly a schema-2 document."
    },
    {
      "file": "legacy-no-schema.json",
      "expect": "accept",
      "why": "Schema 1 predates the `schema` key, so an ABSENT key means 1 — not 'unknown'. A reader that requires the key rejects every manifest ever published before it."
    },
    {
      "file": "future-schema.json",
      "expect": "refuse:schema",
      "why": "A manifest newer than the reader. Must fail with 'update the app', citing the schema — never a parse or validation error."
    },
    {
      "file": "future-unknown-option-kind.json",
      "expect": "refuse:schema",
      "why": "THE ORDERING TEST. Carries an option kind that does not exist, so a reader that parses the document before checking `schema` dies on the unknown kind and reports a parse failure. Correct behaviour is the SAME clean schema refusal as future-schema.json: read `schema` first, decide, only then parse."
    },
    {
      "file": "additive-unknown-keys.json",
      "expect": "accept",
      "why": "Same schema, unknown keys added at the top level, inside a file entry, inside a BUNDLE and inside an option. The producers treat additive keys as backward-compatible and do NOT bump `schema` for them, so a reader MUST ignore what it does not recognise. A reader that errors on unknown fields forces a needless bump for every addition."
    },
    {
      "file": "invalid-bundle-size-sum.json",
      "expect": "refuse:invalid",
      "why": "B2: the members' sizes no longer sum to the bundle's `size`. The reader splits a solid stream by counting bytes, so a wrong total silently misaligns every member after the error — it must be caught up front, not discovered as a hash mismatch."
    },
    {
      "file": "invalid-orphan-member.json",
      "expect": "refuse:invalid",
      "why": "B1: a member hash matching no entry in the document. Its size is therefore unknowable and the stream cannot be split at all."
    },
    {
      "file": "invalid-unbundled-entry.json",
      "expect": "refuse:invalid",
      "why": "B3: an entry with no `name`, a non-zero size, and its hash in no bundle. Nothing in the document says where its bytes come from."
    },
    {
      "file": "invalid-duplicate-member.json",
      "expect": "refuse:invalid",
      "why": "B5: one hash carried by two bundles. The reader would fetch and decode the same content twice, and 'the bundle holding X' stops being well defined."
    },
    {
      "file": "invalid-empty-bundle.json",
      "expect": "refuse:invalid",
      "why": "B7: a bundle with no members — an asset that costs a download and yields nothing."
    },
    {
      "file": "unknown-codec.json",
      "expect": "refuse:codec",
      "why": "A codec this build cannot decode, under a schema it CAN read. Adding a codec is supposed to bump `schema`, so this is a producer that broke that rule — but the user-facing answer is still 'update the app', never 'your download is corrupt'. Must be caught at parse time (R9), not deferred to when the bundle is needed."
    }
  ]
}
